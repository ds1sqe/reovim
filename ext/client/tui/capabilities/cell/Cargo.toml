[package]
name = "reovim-ext-client-tui-cap-cell"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "TUI cell-grid capability type for FrameTarget slots"
repository.workspace = true
keywords.workspace = true
categories.workspace = true

[lints]
workspace = true

[dependencies]
bitflags = { workspace = true }

# Plan 21 / 17-β.2b-impl-a: `impl RenderSurface for CellCapability`
# (ExtClient{tui} → RepoCore edge, allowed by the classifier). Unlocks
# consumer migration: any `&mut dyn RenderSurface` call-site accepts
# a `CellCapability` without call-site changes.
reovim-client-driver = { workspace = true }
