[package]
name = "reovim-driver-text-buffer"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Buffer manager driver for reovim - defines BufferManagerKey and registry"
repository.workspace = true
keywords.workspace = true
categories.workspace = true

[lints]
workspace = true

[dependencies]
# Kernel types for BufferManager trait
reovim-kernel = { workspace = true }

# Buffer type moved to provider-text (#740)
reovim-provider-text = { workspace = true }

# Arch for RwLock (used by TestBufferManager)
reovim-arch = { workspace = true }

# Note: bitflags no longer needed here — BufferCapabilities moved to provider-text
