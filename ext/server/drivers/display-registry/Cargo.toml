[package]
name = "reovim-driver-display-registry"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Server-tier registry contracts for display-driver-side theme/gutter/statusline types"
repository.workspace = true
keywords.workspace = true
categories.workspace = true

[dependencies]
reovim-kernel = { workspace = true }
reovim-arch = { workspace = true }
reovim-driver-annotation = { workspace = true }
reovim-driver-statusline = { workspace = true }

# Platform-specific config directories (theme search paths).
dirs = "5.0"

[dev-dependencies]
# Temporary directories for ThemeLoader tests.
tempfile = "3.14"

[lints]
workspace = true
