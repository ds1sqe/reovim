[package]
name = "reovim-module-treesitter-toml"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "TOML syntax highlighting module for reovim"
repository.workspace = true
homepage.workspace = true

[lints]
workspace = true

[dependencies]
# Kernel layer - for Module trait
reovim-kernel = { workspace = true }

# Driver layer - implements SyntaxDriverFactory
reovim-driver-syntax = { workspace = true }
reovim-driver-syntax-treesitter = { workspace = true }

# Tree-sitter TOML grammar (using -ng fork with newer tree-sitter API)
tree-sitter-toml-ng = "0.7"

# Logging
tracing = { workspace = true }

[dev-dependencies]
reovim-testing = { workspace = true }
