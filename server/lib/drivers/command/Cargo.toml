[package]
name = "reovim-driver-command"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Command driver for reovim (command execution framework)"
repository.workspace = true
keywords.workspace = true
categories.workspace = true

[lints]
workspace = true

[dependencies]
# Subsys layer (command contracts)
reovim-subsys-command = { workspace = true }

# Driver layer (for SessionRuntime in CommandHandler)
reovim-driver-session = { workspace = true }

# Kernel layer (for Service in registry)
reovim-kernel = { workspace = true }
