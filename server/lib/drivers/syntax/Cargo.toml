[package]
name = "reovim-driver-syntax"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Syntax highlighting driver for reovim (trait definitions only)"
repository.workspace = true
keywords.workspace = true
categories.workspace = true

[lints]
workspace = true

[dependencies]
# Subsystem contracts (traits, types, state)
reovim-subsys-syntax = { workspace = true }

# Text domain events (for text_event_to_syntax_edit bridge in bridge.rs)
reovim-domain-text-events = { workspace = true }

# Kernel layer (needed by bridge.rs)
reovim-kernel = { workspace = true }
