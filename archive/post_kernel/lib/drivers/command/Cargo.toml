[package]
name = "reovim-driver-command"
version = "0.9.2-dev"
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Command driver for reovim (command execution framework)"
repository.workspace = true
keywords.workspace = true
categories.workspace = true

[lints]
workspace = true

[dependencies]
# Kernel layer (for CommandId)
reovim-kernel = { path = "../../kernel", version = "0.9.2-dev" }

# Driver layer
reovim-driver-command-types = { path = "../command-types", version = "0.9.2-dev" }
reovim-driver-vfs = { path = "../vfs", version = "0.9.2-dev" }
reovim-driver-display = { path = "../display", version = "0.9.2-dev" }
reovim-driver-session = { path = "../session", version = "0.9.2-dev" }
