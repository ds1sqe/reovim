[package]
name = "root-05"
reovim-version = "^0.15"

[dependencies]
eager-dep = { path = "eager-dep" }
lazy-dep = { path = "lazy-dep" }

[lazy]
eager-dep = { eager = true }
lazy-dep = { on-domain = "text" }
