[package]
name = "reovim-domain-mesh"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Mesh content domain scaffold for reovim (#753 Flight 77). Proves the zero-edit-for-new-domain invariant beyond the text domain."
repository.workspace = true
keywords.workspace = true
categories.workspace = true

[lints]
workspace = true

[dependencies]
# Domain trait for content-type abstraction. This is the ONLY
# non-test dep — the zero-edit invariant depends on it staying
# exactly one line.
reovim-domain = { workspace = true }

[dev-dependencies]
# Used by tests/domain_neutrality.rs to compile the Text domain
# alongside Mesh against a single generic function.
reovim-domain-text = { workspace = true }
