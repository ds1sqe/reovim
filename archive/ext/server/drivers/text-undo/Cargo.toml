[package]
name = "reovim-driver-text-undo"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Undo provider driver for reovim - defines UndoProvider trait and registry"
repository.workspace = true
keywords.workspace = true
categories.workspace = true

[lints]
workspace = true

[dependencies]
# Kernel types for BufferId, UndoResult, UndoTree
reovim-kernel = { workspace = true }
# Text types (Edit, Position)
reovim-domain-text = { workspace = true }

# VFS driver for persistence methods
reovim-subsys-vfs = { workspace = true }

# Logging for load_graceful default implementation
tracing = { workspace = true }
