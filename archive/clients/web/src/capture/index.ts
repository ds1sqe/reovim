/**
 * Capture Module
 *
 * Provides frame capture functionality for the web client.
 * Used by both browser Editor and Node.js HeadlessWebClient.
 */

export {
  CaptureHandler,
  type CaptureFormat,
  type CaptureableState,
  type CaptureHandlerConfig,
  type FrameCapture,
  type FrameMetadata,
  type WindowState,
} from "./handler.js";
