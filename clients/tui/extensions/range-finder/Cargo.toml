[package]
name = "reovim-tui-ext-range-finder"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Range-finder TUI extension (jump labels + fold indicators) for reovim"
repository.workspace = true
homepage.workspace = true

[lints]
workspace = true

[dependencies]
# Display driver (RenderBackend, TuiExtension, Style, ViewportContext)
reovim-driver-display = { workspace = true }

# Platform abstraction (Color)
reovim-arch = { workspace = true }

# Shared extension kind constants (#583)
reovim-extension-kinds = { workspace = true }

# JSON parsing for bridge data
serde_json = { workspace = true }
