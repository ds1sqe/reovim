[package]
name = "reovim-tui-ext-diagnostics"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Diagnostics inline TUI extension for reovim"
repository.workspace = true
homepage.workspace = true

[lints]
workspace = true

[dependencies]
# Display driver (RenderBackend, TuiExtension, Style, ViewportContext)
reovim-driver-display = { workspace = true }

# Platform abstraction (Color)
reovim-arch = { workspace = true }

# Typed JSON deserialization
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
