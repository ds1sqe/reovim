[package]
name = "reovim-tui-ext-hover"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Hover popup TUI extension for reovim"
repository.workspace = true
homepage.workspace = true

[lints]
workspace = true

[dependencies]
# Display driver (RenderBackend, TuiExtension, Style)
reovim-driver-display = { workspace = true }

# Platform abstraction (Color)
reovim-arch = { workspace = true }

# Shared extension kind constants (#583)
reovim-extension-kinds = { workspace = true }

# Typed JSON deserialization
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
