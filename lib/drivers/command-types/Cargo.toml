[package]
name = "reovim-driver-command-types"
version = "0.9.1-dev"
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Command types for reovim (shared between command and session drivers)"
repository.workspace = true
keywords.workspace = true
categories.workspace = true

[lints]
workspace = true

[dependencies]
# Kernel layer (for BufferId)
reovim-kernel = { path = "../../kernel", version = "0.9.1-dev" }

# Driver layer (for VfsDriver)
reovim-driver-vfs = { path = "../vfs", version = "0.9.1-dev" }
