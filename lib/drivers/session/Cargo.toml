[package]
name = "reovim-driver-session"
version = "0.9.1-dev"
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Session driver for reovim - provides traits for session management"
repository.workspace = true
keywords.workspace = true
categories.workspace = true


[dependencies]
# Kernel types for ModeId, BufferId, etc.
reovim-kernel = { path = "../../kernel" }
# Command types for CommandContext, CommandResult
reovim-driver-command = { path = "../command" }

[lints]
workspace = true
