[package]
name = "reovim-driver-clipboard"
version = "0.9.1-dev"
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Clipboard provider driver for reovim - system clipboard and yank history"
repository.workspace = true
keywords.workspace = true
categories.workspace = true

[lints]
workspace = true

[dependencies]
# Kernel types for RegisterContent, YankType
reovim-kernel = { path = "../../kernel", version = "0.9.1-dev" }
