[package]
name = "reovim-pkg-resolver"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Semver graph resolution over pkg.toml + local-path dependency trees for the reovim package manager"
repository.workspace = true
keywords.workspace = true
categories.workspace = true

[lints]
workspace = true

[dependencies]
reovim-pkg-manifest = { path = "../pkg-manifest", version = "0.15.0-dev" }
reovim-pkg-lockfile = { path = "../pkg-lockfile", version = "0.15.0-dev" }
semver = { workspace = true }
thiserror = "2"

[dev-dependencies]
tempfile = "3"
