# Changelog

For old changelog, see `changelog/CHANGELOG-{version}.md`

## [0.15.0-dev] - Unreleased

### Breaking Changes (Internal)

- **server**: `reovim-domain-text`, `reovim-domain-text-events`, and `reovim-provider-text` removed from `reovim-server` entirely (both `[dependencies]` and `[dev-dependencies]`). Server is now a domain-agnostic dispatch layer with zero domain crate imports in production and test code. All text-domain types accessed via driver re-exports (`reovim-driver-session`, `reovim-driver-buffer`, `reovim-driver-codec`). Depgraph guard test `server_no_domain_text` checks all dependency kinds (normal + dev)
- **server**: `ResolveResult::InsertChar` no longer handled by server dispatch. Resolvers now insert characters directly through `SessionApiDyn`. Server `InsertChar` arm is defensive no-op with error logging
- **server**: `FallbackContext` impl removed from `AppState`
- **kernel**: `reovim-domain-text` removed from kernel dependency graph entirely. ~5,190 lines of text-domain tests relocated
- **kernel**: `UnsavedBuffer.line_count: usize` replaced with `content_size: u64`
- **kernel**: `CommandRegistration` capability flags renamed to domain-neutral vocabulary
- **drivers**: 12 pure-contract driver crates renamed from `reovim-driver-*` to `reovim-subsys-*` and moved from `server/lib/drivers/` to `server/lib/subsys/`. Affected crates: annotation, clipboard, completion, formatter, git, layout, manifest, module-config, module-loader, module-registry, statusline, vfs. All `use reovim_driver_{name}` imports must change to `use reovim_subsys_{name}`
- **drivers**: Session contract types extracted from `reovim-driver-session` into new `reovim-subsys-session` crate. Moved items: ClientId, Viewport, CursorSnapshot, KeySequence, SessionExtension, ExtensionMap, SessionMode, EmptySessionHandler, SessionHandlerKey/Registry, InitialModeProvider, LeaderKeyProvider, PendingNotificationQueue, StaleCheck, TickScheduler, ClipboardApi, ExtensionApi, FindCharRecord, CompositorApi, BridgeRegistry/Provider. Driver-session re-exports all items for backwards compatibility
- **drivers**: Syntax contract types extracted from `reovim-driver-syntax` into new `reovim-subsys-syntax` crate. All core traits and types moved (SyntaxDriver, SyntaxDriverFactory, LanguageRegistry, SyntaxCache, SyntaxEdit, HighlightCategory, Annotation, FoldRange, etc.). Only the domain-specific bridge function `text_event_to_syntax_edit` remains in driver-syntax. Driver-syntax re-exports all items for backwards compatibility
- **drivers**: `reovim-driver-command-types` renamed to `reovim-subsys-command-types` and moved to `server/lib/subsys/command-types/`. Domain dependency removed: `CommandContext::cursor_position()` now returns `Option<(usize, usize)>` instead of `Option<Position>`, `set_cursor_position()` now takes `(line: usize, col: usize)` instead of `Position`. All `use reovim_driver_command_types` imports must change to `use reovim_subsys_command_types`
- **drivers**: Command metadata traits extracted from `reovim-driver-command` into new `reovim-subsys-command` crate. Moved items: Command, CommandPriority, CommandInfo, CommandQueryProvider, CommandQueryService, AmbiguousPrefix, CommandNameIndex, bind_args, parse_cmdline, tokenize_args. CommandHandler, CommandHandlerStore, and CommandProvider remain in driver-command (they reference SessionRuntime). Driver-command re-exports all items for backwards compatibility
- **drivers**: Domain-free input contract types extracted from `reovim-driver-input` into new `reovim-subsys-input` crate. Moved items: KeyCode, KeyEvent, KeyEventKind, KeymapResult, Modifiers, MouseButton, MouseEvent, MouseEventKind, ClipboardError, InputError, ClipboardProvider, KeySequence, Keybinding, KeybindingTarget, BindingLayer, EagerLookupPolicy, KeyLookupPolicy, KeyLookupResult, KeyLookupState, KeymapQuery, ModeLifecycleHandler, NopLifecycleHandler, DefaultModeProvider, ProviderPriority, DefaultModeProviderModule, ModeProviderKey, ModeProviderRegistry, ModeInfo, ModeInfoStore, KeybindingStore, LookupPolicyStore, PendingBindings, BindingInfo. Driver-input re-exports all moved items for backwards compatibility; resolver, fallback, and resolver_registry remain in driver-input

- **server**: All 6 remaining driver crate dependencies (`driver-text-buffer`, `driver-codec`, `driver-command`, `driver-text-input`, `driver-text-session`, `driver-text-syntax`) removed from `[dependencies]` and moved to `[dev-dependencies]` only (#753 E6). Server production code has zero driver imports. `DriverSession` dissolved: `compositor` and `home_mode` are direct `SessionState` fields. `CommandRegistry` stores `Arc<dyn Command>` (subsys) instead of `Arc<dyn CommandHandler>` (driver); execution routes through `DomainDriver`. `ClientPresence` text-domain fields (`cursor`, `visible_lines`, `mode`) removed. Buffer/syntax/codec RPCs stubbed with `Unimplemented` pending domain driver wiring
- **server**: `reovim-driver-undo` and `reovim-driver-search` removed from server dependencies (zero production usage). Server imports for subsys-available types switched from driver crates to subsys crates (subsys-session, subsys-command, subsys-command-types, subsys-input, subsys-syntax). Remaining driver deps (session, input, command, buffer, codec, syntax) are domain-locked and documented for Tier 2 decoupling
- **drivers**: `reovim-subsys-syntax` dissolved back into `reovim-driver-syntax`. Syntax is a text-domain concept and does not belong in the domain-neutral subsys layer. All source modules moved from `server/lib/subsys/syntax/` into `server/lib/drivers/syntax/`. All `use reovim_subsys_syntax` imports must change to `use reovim_driver_syntax`
- **server**: Test suite migrated to proto v3 and domain-neutral architecture (#753 E6 Phase 5). 8330+ lines of dead test code deleted across 18 files. Tests for removed v2 RPCs (GetRawContent, GetLineCount, GetAnnotations, codec RPCs, DebugGetMode, Resize), dead inline dispatch helpers, and driver-coupled command execution tests eliminated. 663 lib tests pass. `CommandRegistry` tests rewritten as metadata-only. `SyntaxStreamState` infra tests kept, domain-logic tests moved to driver crate
- **server**: All 6 driver crates removed from `[dev-dependencies]` (#753 G-chain). Server test suite now imports only kernel + subsys types. Driver-coupled test imports rewritten to use subsys re-exports (`reovim_subsys_session`, `reovim_subsys_input`, `reovim_subsys_command`). 8 tests using `TestBufferManager`/`CommandHandler`/`SessionRuntime` deleted. 655 lib tests pass with zero driver references in entire server crate
- **protocol**: Proto v3 replaces v2 (`package reovim.v3`). 12 fresh proto files in `shared/protocol/proto/reovim/v3/`. All text-domain types removed: `BufferChange`, `LineAnnotation`, `CodecMetadata`, `GetRawContent`, `GetLineCount`, `GetAnnotations`, `GetVisibleLines`, `GetTokens`, `GetLanguageInfo`, all codec RPCs, `DebugGetMode`. `syntax.proto` removed entirely (tokens flow as projections). `SendInput` replaces `SendKeys` (opaque bytes payload). `SurfaceChanged` replaces `Resize` (opaque `SurfaceDescriptorProto`). `BufferOpenedPayload`/`BufferClosedPayload` replace `BufferModifiedPayload`+`BufferChange`. `WindowInfo` extended with `primary_domain_id`, `embedded_domain_ids`, `spatial_placement`. `ClientPresence` extended with `spatial_state`. `JoinResponse` extended with `domains` and `surface`. `BufferInfo.line_count` and `codec_metadata` removed. All `reovim_protocol::v2::` imports must change to `reovim_protocol::v3::` (bridge in place during migration)
- **subsys**: `reovim-driver-net` renamed to `reovim-subsys-net` and relocated from `shared/net/` to `server/lib/subsys/net/`. Semantic correction — the crate defines subsys-level trait contracts (`NetDriver`, `TransportListener`, `TransportConnection`, `PortAllocator`, `RpcHandler`), not driver implementations. Workspace alias `reovim-net` renamed to `reovim-subsys-net` (previously mismatched against the old package name). Zero production consumers affected; transport driver implementation deferred to a follow-on plan
- **arch**: `reovim-arch` relocated from `shared/arch/` to top-level `arch/` (Linux-inspired platform abstraction layer). Package name unchanged (`reovim-arch`). Workspace dep path updated; `scripts/publish.sh` updated. Consumers unaffected since all imports use crate names, not paths
- **lib**: `reovim-depgraph` relocated from `shared/depgraph/` to top-level `lib/depgraph/` (Linux-inspired core library layout). Package name unchanged. Workspace dep path updated; `scripts/publish.sh` updated. Consumers unaffected since all imports use crate names, not paths
- **apps**: `reovim-driver-log` crate dissolved — tracing subscriber setup absorbed into `apps/bin/src/logging.rs` as application bootstrap. Only `apps/bin` consumed it, and kernel `Logger` implementation was already `reovim_server::debug::CompositeLogger`. Dead code removed: `TracingLogger`, `to_tracing_level`, `from_tracing_level`, and ~900 lines of tests covering the unused Logger bridge. Workspace member, workspace dep, `scripts/publish.sh` entry, and `docs/architecture/drivers/log/` removed
- **lib**: `reovim-capabilities` and `reovim-bench-utils` relocated from `shared/` to `lib/` (pure crates, zero reovim deps). `lib/capabilities` is marked TEMP and will move to subsys or uapi in a follow-on phase. Package names and consumers unchanged
- **uapi**: `reovim-protocol` relocated from `shared/protocol/` to `uapi/protocol/` and `reovim-render-codec` relocated from `shared/render-codec/` to `uapi/render-codec/` — new Linux-inspired top-level `uapi/` for stable wire contracts (gRPC proto, CommandBuffer encoding). Package names unchanged. `.codecov.yml` path updated for generated proto files
- **apps**: `reovim-driver-trace` crate dissolved — `TracingProfiler` + `init_profiling` absorbed into `apps/bin/src/profiling.rs`. Now wired at startup so `profile_scope!` calls in server code emit flame-graph spans when `REOVIM_PROFILE` is set (previously the Profiler trait was unwired and profile_scope! expansions were no-ops). Kernel-side profiler infrastructure unchanged
- **uapi**: `reovim-module-macros` relocated from `shared/module-macros/` to `uapi/module-macros/`. `declare_module!` and `declare_client_module!` define the module FFI ABI — the binding interface between runtime and extensions — which is a stable contract, the uapi layer's role. Package name unchanged
- **tools**: `reovim-testing` integration test harness relocated from `shared/testing/` to `tools/testing/` (Linux-inspired; dev-time tooling not shipped in binaries). Consumers are all `dev-dependencies`. Workspace members, workspace dep path, `scripts/publish.sh`, `.codecov.yml`, and LSP fixture path in `server/lib/server/tests/lsp_diagnostics.rs` updated. Active docs (`docs/contributing/guides/testing.md`, `docs/user-guide/python-modules.md`) updated
- **tools**: `tools/reovim-testing/` (Python test SDK) renamed to `tools/python-test-sdk/` to disambiguate from the Rust `tools/testing/` harness. Rust and Python ecosystems both used the same crate/package name `reovim-testing`, creating directory-level ambiguity. `Cargo.toml` `exclude` entry updated
- **clients**: `reovim-client-model` and `reovim-client-driver` relocated from `shared/clients/{model,driver}/` to `clients/lib/{model,driver}/` — both are client-core shared infra (platform-agnostic model types and the client module ABI) consumed by the `clients/tui/` core app and its modules. Placing them under `clients/lib/` keeps them core-tier; the `ext/client/lib/` target in Plan 13 was wrong because a `clients/<platform>/` core → `ext/client/lib/` edge would be a core→ext violation. Package names unchanged. Workspace members, workspace dep paths, `scripts/publish.sh`, `clients/web/package.json` WASM build script, `clients/web/.gitignore`, and active docs (`docs/architecture/server-client-split.md`) updated. Test-local path comments updated
- **ext**: `reovim-domain` and `reovim-domain-text` (+ its sibling `reovim-domain-text-events`) relocated from `shared/domain/` and `shared/domains/text/` to `ext/server/domain/domain/` and `ext/server/domain/text/` — establishes the `ext/` top-level tree and the `ext/server/domain/` subtree for pluggable content domains. Package names unchanged
- **uapi**: `reovim-input-codec` relocated from `shared/input-codec/` to `uapi/input-codec/` as a temporary home pending the Plan 15 input-opaque redesign. Fits `uapi/` role (wire contract: encode/decode for InputEvent opaque payloads). Package name unchanged
- **shared**: `shared/` directory removed entirely. All former contents now live under `arch/`, `lib/`, `uapi/`, `tools/`, `clients/lib/`, or `ext/server/domain/`
- **ext**: `reovim-driver-tui` and `reovim-driver-display` relocated from `clients/tui/lib/drivers/{tui,display}/` to `ext/client/tui/drivers/{tui,display}/`. Establishes the `ext/client/tui/` subtree for TUI-scoped platform extensions. Empty `clients/tui/lib/` removed. Workspace members + deps paths, `scripts/publish.sh`, and active docs (`docs/architecture/drivers/overview.md`, `docs/architecture/client/tui.md`) updated. In-source doc path comments in display driver and web extension interface updated
- **ext**: All 20 TUI client modules (`bufferline`, `cmdline`, `completion`, `diagnostics`, `explorer`, `fold`, `hover`, `illuminate`, `jump`, `landing`, `line-numbers`, `markdown`, `microscope`, `notification`, `pair`, `signature-help`, `statusline`, `tetromino`, `which-key`, `yank-flash`) relocated from `clients/tui/modules/` to `ext/client/tui/modules/`. Package names unchanged (`reovim-tui-mod-*`). Workspace members + deps paths, `scripts/publish.sh`, `docs/architecture/client/tui.md`, and `clients/web/src/extensions/yank-flash.ts` path reference updated
- **ext**: All 19 server driver crates (`annotation`, `codec`, `codec-xxd`, `command`, `completion`, `formatter`, `git`, `picker`, `statusline`, `syntax-treesitter`, `text-buffer`, `text-ffi`, `text-ffi-python`, `text-input`, `text-lsp`, `text-search`, `text-session`, `text-syntax`, `text-undo`) relocated from `server/lib/drivers/` to `ext/server/drivers/`. Package names unchanged. Workspace members + deps paths, `scripts/publish.sh`, `.codecov.yml`, and ~22 active architecture/contributing/user-guide docs updated. In-source doc comments in drivers, kernel api/service.rs, subsys-net, providers/text, depgraph session_no_text test, and test fixtures updated
- **ext**: All 73 server module crates relocated from `server/modules/` to `ext/server/modules/`. Package names unchanged (`reovim-module-*`). Workspace members + deps paths, `scripts/publish.sh`, and all active architecture/contributing/user-guide docs and in-source path comments updated (active tree only — heritage docs and historical changelog entries unchanged)
- **lib**: `reovim-domain` (generic Domain trait, zero deps) promoted from `ext/server/domain/domain/` to top-level `lib/domain/`. The trait contract that enables `Buffer<D>`, `Decode<D>`, `Encode<D>`, etc. is the *definition* of domain-neutrality and belongs in core — placing it under `ext/` was a category error in the Phase 13 bulk relocation. `reovim-domain-text` (the text-domain implementation) remains at `ext/server/domain/text/`. Package name unchanged; consumers unaffected since imports use the crate name. Workspace members + deps paths and active docs updated
- **docs**: Documentation path sweep — all active docs (`CLAUDE.md`, `docs/architecture/*`, `docs/contributing/*`, `docs/user-guide/*`) updated to reflect the post-#753 core/ext workspace layout. Mechanical substitutions: `shared/{protocol,render-codec,input-codec,module-macros}/` → `uapi/*`; `shared/{arch}/` → `arch/`; `shared/{depgraph,log,trace,bench,capabilities}/` → `lib/*`; `shared/net/` → `server/lib/subsys/net/`; `shared/testing/` → `tools/testing/`; `shared/clients/` → `clients/lib/`; `shared/domains/text/` → `ext/server/domain/text/`; `server/lib/drivers/` → `ext/server/drivers/`; `server/lib/providers/` → `ext/server/providers/`; `server/modules/` → `ext/server/modules/`; `clients/tui/{lib/drivers,modules}/` → `ext/client/tui/{drivers,modules}/`. Heritage docs (`docs/heritage/*`) untouched (historical record). `CLAUDE.md` architecture section rewritten with the post-#753 layer model and core/ext boundary rules

### Added

- **Plan 16 Phase A — capabilities decentralization pilot** (#753): three low-fan-out capabilities moved from `lib/capabilities/` to the subsys that owns the trait they advertise. Pattern: new `capabilities.rs` + sibling `capabilities_tests.rs` module inside each subsys crate; consumer import becomes `reovim_subsys_<name>::capabilities::<CONST>`. Verbatim copy — capability strings unchanged.
  - `reovim-subsys-clipboard`: `capabilities::CLIPBOARD_PROVIDER = "clipboard-provider"`. Consumer `ext/server/modules/clipboard` migrated; its `reovim-capabilities` dep removed (already depended on `reovim-subsys-clipboard`).
  - `reovim-subsys-command`: `capabilities::COMMAND_DISPATCH = "command-dispatch"`. Consumer `ext/server/modules/commands` migrated; `reovim-capabilities` dep removed and `reovim-subsys-command` dep added (was only depending on `subsys-command-types` before).
  - `reovim-subsys-vfs`: `capabilities::VFS_PROVIDER = "vfs-provider"`. Consumer `ext/server/modules/vfs-local` migrated; `reovim-capabilities` dep removed.
  - Each subsys gets 2 new tests (`capabilities_are_kebab_case`, `capabilities_have_no_duplicates`) matching the Decision 4 canonical shape. `lib/capabilities/` still exists with 14 remaining constants (deletion is Plan 16 Phase E).

- **Plan 16 Phase B — CODEC_PROVIDER decentralization** (#753): the content-codec capability moved from `lib/capabilities/` to `reovim-subsys-content-codec`, scaling the Phase A canonical pattern to 9 consumer crates. New `capabilities.rs` + sibling `capabilities_tests.rs` in the subsys; consumer import becomes `reovim_subsys_content_codec::capabilities::CODEC_PROVIDER`. Verbatim copy — capability string unchanged.
  - `reovim-subsys-content-codec`: `capabilities::CODEC_PROVIDER = "codec-provider"`. 2 new tests (`capabilities_are_kebab_case`, `capabilities_have_no_duplicates`).
  - 9 consumer crates migrated: `reovim-content-codec-{binary-struct, cjk, csv, hex, legacy, pdf, rlib, tar-gz, utf8}`. Each removed its `reovim-capabilities` Cargo dep (already depended on `reovim-subsys-content-codec`).
  - 730 tests pass (12 subsys + 718 consumer). `lib/capabilities/` still exists with 13 remaining constants (deletion is Plan 16 Phase E).

- **Plan 16 Phase C — subsys-input capabilities decentralization** (#753): the two input-domain capabilities (`MODE_MANAGEMENT`, `MOTION_COMMANDS`) moved from `lib/capabilities/` to `reovim-subsys-input`. Canonical Decision-4 pattern applied: `pub mod capabilities;` + sibling `capabilities_tests.rs` (kebab-case + no-duplicates). Consumer imports become `reovim_subsys_input::capabilities::<CONST>`.
  - 4 consumer modules migrated: `mode-manager`, `vim`, `emacs` (lib.rs + lib_tests.rs), `motions`. Each Cargo.toml swaps `reovim-capabilities` for `reovim-subsys-input`.
  - `lib/capabilities/` now has 11 remaining constants (all text-domain; Phase D).

- **Plan 16 Phase D — text-domain capabilities decentralization** (#753): the 11 text-domain capabilities moved from `lib/capabilities/` to a new zero-dep crate `reovim-domain-text-capabilities` at `ext/server/domain/text/capabilities/`. Per Decision 2 + Q1 resolution, capability strings now live beside the domain implementation rather than in a god crate.
  - New crate with 11 constants (9 active + 2 ride-along): `BUFFER_MANAGER`, `COMPLETION_PROVIDER`, `FILE_EXPLORER` (dead-code), `FORMATTER_PROVIDER`, `FUZZY_FINDER` (dead-code), `GIT_PROVIDER`, `LSP_PROVIDER`, `SEARCH_PROVIDER`, `SNIPPET_PROVIDER`, `SYNTAX_HIGHLIGHTING`, `UNDO_PROVIDER`. 5 unit tests (count=11, sorted, no-duplicates, contains-each, kebab-case).
  - 10 consumer modules migrated: `buffer-simple`, `lsp`, `completion`, `git`, `format` (lib.rs + lib_tests.rs), `snippet`, `treesitter-rust`, `undo`, `search`, `treesitter-markdown`. Each Cargo.toml swaps `reovim-capabilities` for `reovim-domain-text-capabilities`.
  - 1129 consumer tests pass; 5 new-crate tests pass. `lib/capabilities/` now has zero production consumers; deletion happens in Phase E.

- **uapi/input-codec**: Reshaped to CLOSED mechanism only (Plan 14 I.1). Owns `InputEvent` opaque envelope, `INPUT_HEADER_SIZE`, header accessors (`input_kind`/`input_flags`/`input_context`), `InputFlags`, `InputPayloadError`, `Codec` trait (object-safe), and `InputSequence` (Vec<Vec<u8>> byte-sequence key). Platform vocabulary (`KeyCode`, `Modifiers`, `MouseEvent`, etc.) removed — now lives in `ext/input-codec/tui/`. `InputPayloadError` variants extended with `WrongType { expected: &'static str }` and `InvalidData { reason: &'static str }` for codec encode/decode diagnostics
- **subsys-input**: Extended with `InputCodecRegistry` trait + `DefaultInputCodecRegistry` impl (parking_lot RwLock, concurrent-safe) (Plan 14 I.2). Generic `LookupState<C>`, `LookupResult<C>`, `LookupPolicy<C>` trait, `EagerLookupPolicy`, `KeymapQuery<C>`, `BindingInfo`, `BindingLayer` moved here from the deleted subsys-input-contracts
- **ext/input-codec/tui**: New `reovim-codec-tui-input` crate — TUI platform input codec module (Plan 14 I.3). Owns `KeyCode`, `Modifiers`, `KeyEvent`, `KeyEventKind`, `KeymapResult`, `MouseButton`, `MouseEvent`, `MouseEventKind`, `ScrollEvent` vocabulary. Provides `TuiKeyCodec` (KIND=0x0001), `TuiMouseCodec` (KIND=0x0002), `TuiScrollCodec` (KIND=0x0003) implementing the `Codec` trait from `reovim-input-codec`. `declare_module!` registers all three codecs with `InputCodecRegistry` on `init`; `TuiInputCodecModule` holds the registry `Arc` so `exit()` unregisters symmetrically (codec lifetime = module lifetime). Zero dependency on `reovim-arch`
- **server/input**: Vim-notation parser deleted from `server/lib/server/src/grpc/input.rs` (Plan 14 I.5). Server accepts opaque `InputEvent` payloads and dispatches unchanged to the domain driver. Server registry switched to `InputSequence` byte-sequence keys and generic `LookupResult<CommandId>` (Plan 14 I.7). `EditingState::pending_keys: KeySequence` renamed to `pending_input: InputSequence`. `server/lib/server/Cargo.toml` `[dependencies]` AND `[dev-dependencies]` have ZERO input-vocab / codec crates — server is strictly platform-agnostic. Server tests use opaque synthetic byte payloads; codec-semantic tests live in `ext/input-codec/tui/` where the codec is defined
- **clients**: TUI and CLI clients migrated to encode input via `reovim-codec-tui-input::encode_key_event` instead of sending UTF-8 notation strings (Plan 14 I.4)
- **depgraph**: Layer enforcement tests — `subsys_no_domain`, `subsys_no_driver`, `subsys_no_name_leak`, `server_no_domain`, `server_no_driver` guard the subsys/driver layer separation
- **subsys**: New `reovim-subsys-coordination` crate — domain-neutral Position and Cursor traits with `[u8; 8]` header codec pattern (`domain_id(u32) + inner_id(u16) + flags(u16)`), PositionCodec/CursorCodec decode traits, CoordinationRegistry for dynamic driver enlistment. Zero dependencies. Enables multi-domain editing (text, mesh, image) without server changes
- **subsys**: Domain-neutral session contracts in `reovim-subsys-session` — `DomainDriver` trait (what each domain implements: dispatch, lifecycle, focus, cursors, content), `DomainStateQuery` optional trait (mode, registers, status), `ChangeSet` domain-neutral change tracking (boolean flags + ID lists, no text-domain fields), `BufferContentProvider` service trait (Arc-compatible, object-safe), domain-neutral `Window` struct with cached opaque `Vec<Box<dyn Cursor>>` handles. Enables multi-domain sessions (SessionRuntime = mechanism, DomainDriver = policy). Zero domain/driver dependencies
- **drivers**: `PendingBindings` moved from `reovim-subsys-input` to `reovim-driver-input`. Breaks `subsys-input → subsys-session` circular dependency, enabling `subsys-session → subsys-input` for `KeyEvent` in `DomainDriver`. `reovim-subsys-session` dep removed from `reovim-subsys-input`. All `use reovim_subsys_input::PendingBindings` imports must change to `use reovim_driver_input::PendingBindings`
- **drivers**: Text-domain `DomainDriver` implementation in `reovim-driver-text-session` — `TextDomainDriver` struct wrapping `SessionRuntime` with `RwLock` interior mutability, `TextCursor`/`TextPosition` implementing `dyn Cursor`/`dyn Position` with text-domain codec encoding, `TextContentProvider` implementing `BufferContentProvider` by wrapping `TextBufferRegistry`, `TextClientState` per-client text state bundle as `SessionExtension`, `state_changes_to_change_set` bridge from text-domain `StateChanges` to domain-neutral `ChangeSet`
- **drivers**: 9 text-domain driver crates renamed from `reovim-driver-*` to `reovim-driver-text-*` to make domain scope explicit. Renamed crates: buffer, input, search, syntax, undo, lsp, ffi, ffi-python, session. Domain-neutral drivers unchanged: codec, codec-xxd, picker, syntax-treesitter, command. All `use reovim_driver_{name}` imports must change to `use reovim_driver_text_{name}`

### Changed

- **Plan 15 Phase V.A — mechanical half of proto v2 alias removal** (#753): bulk migration of non-TUI consumers to `reovim_protocol::v3`, vim module macros retargeted off `reovim-input-codec`, TUI `WindowInfo` initializers updated with Plan 14 domain fields, and `clients/tui/src/adapter/panel.rs` refactored off removed display-driver layout primitives.
  - **V.1 bulk rename**: `reovim_protocol::v2::` → `v3::` across 28 files (`apps/bin`, `server/lib/server` + `grpc/*.rs`, `session/*`, `clients/cli`, `clients/lib/driver/projection/transpiler`, `tools/testing/presence`, `ext/server/modules/explorer` test). Excludes `clients/tui/**` (Cluster A/B scope, V.B) and `uapi/protocol/src/lib.rs` (alias lives there until V.7).
  - **V.2 vim-module macros retarget**: `ext/server/modules/vim/src/macros.rs` imports `KeyCode`/`KeyEvent`/`KeyEventKind`/`Modifiers` from `reovim_codec_tui_input` (was `reovim_input_codec`). The removed `key_sequence_to_key_events` helper replaced with inline `notation_token_to_key_event` + `parse_bracket_notation` (registry-free, identical logic to the existing private helper in `ext/server/drivers/text-input/src/input_sequence_bridge.rs`). `reovim-input-codec` → `reovim-codec-tui-input` in the crate's Cargo.toml. 41 macros tests + 1,627 vim-crate tests green.
  - **V.3 `WindowInfo` domain fields**: added `primary_domain_id: 0`, `embedded_domain_ids: vec![]`, `spatial_placement: None` to 4 struct-literal sites (`clients/tui/src/core_helpers.rs:36,141`, `clients/tui/src/app.rs:438,468`). TUI-synthesized default windows have no domain affinity.
  - **V.3a `adapter/panel.rs` layout refactor**: `View`, `ColIndex`, `LineIndex` were removed from `reovim_driver_display::layout` during the post-#753 display reshape (they survived only as a broken import). `TuiPanel` refactored to hold flat fields (`buffer_id`, `scroll_top`, `cursor_line`, `cursor_col`) directly; `panel_tests.rs` updated to match. Single import `reovim_driver_display::BufferId` remains.
  - Workspace compile-error count drops from 53 TUI + 1 vim-module to 48 TUI + 0 vim-module; remaining 48 are strictly Cluster A (deleted RPCs like `GetModeRequest`), Cluster B (`option_changed_payload` / `TokenUpdate`), and the V.4 `peers_v2` rename — all reserved for Phase V.B. Zero touches to `uapi/protocol/src/lib.rs`, `clients/tui/src/grpc_client.rs`, or `clients/tui/src/notification_handler.rs`.

- **Plan 15 Phase V.B — semantic half of proto v2 alias removal** (#753): TUI migrated off removed v2 RPCs and notification payloads; `pub use v3 as v2` alias deleted; bootstrap.rs latent defects (Plan 14 Phase I follow-on masked by upstream clients/tui failures) fixed as part of the same landing since alias deletion surfaces them.
  - **V.4 peers + JoinRequest.surface**: `clients/tui/src/app.rs:1200` renames `join_resp.peers_v2` to `peers`; peer cursor/mode initialize to defaults (Option A — rely on subsequent projection stream). `clients/tui/src/grpc_client.rs:845` `JoinRequest` construction adds `surface: None` to match the v3 `presence.proto` shape.
  - **V.5 Cluster A RPC deletion**: `TuiGrpcClient` strips `get_mode`, `get_mode_for_client`, `get_cursor`, `get_cursor_for_client`, `get_raw_content`, `resize`, `get_selection`, `get_visible_lines`, `get_tokens`, `stream_tokens`. New `get_projections(&[&str], Option<u64>) -> Vec<ProjectionEntry>` helper replaces query paths. `resize(w, h)` call sites migrate to `EditorService::surface_changed` with a `SurfaceDescriptorProto` body encoded via `reovim-surface-codec-tui`. `clients/tui/Cargo.toml` adds `reovim-surface-codec-tui` as a direct production dependency (permitted under the core/ext boundary: TUI core may depend on its own platform's ext tree).
  - **V.6 Cluster B notification handler → `ProjectionUpdated` dispatch**: six removed payload arms (`ModeChanged`, `CursorMoved`, `BufferModified`, `OptionChanged`, `SelectionChanged`, `ResizeRequest`) replaced with one `ProjectionUpdated(p)` arm that dispatches by `p.tag` into per-tag handlers in new `clients/tui/src/projection_handlers.rs`. Live path: `text.mode` decodes raw UTF-8 bytes and updates local/remote client state. Stub paths: `text.cursor`, `text.selection`, `text.buffer_modified`, `options.changed` trace-log only; each carries a `// TODO:` marker pointing at `tmp/deferral-draft-projection-emitters.md` for server-side emitter work. Unknown tags `tracing::trace!` without error. `SurfaceChanged(p)` gains a resize-request semantic replacement arm. 10 new projection-handler unit tests (1 live UTF-8 roundtrip, 4 stub no-panic, 5 helper/edge tests — exceeds the ≥6 requirement).
  - **V.7 alias deletion**: `pub use v3 as v2;` removed from `uapi/protocol/src/lib.rs`. Module docstring updated to reference v3-only (`StateServiceClient` + `GetProjectionsRequest` example). No residual `v2::` references in the workspace.
  - **V.8 TUI test migration**: test files migrated to v3 via the Plan 15 V.8 substitution table. `clients/tui/src/notification_handler_tests.rs` rewrites notification fixtures against `Payload::ProjectionUpdated` shape; residual `SelectionState` reference resolved by local import. `clients/tui/src/render_engine_tests.rs`, `core_helpers_tests.rs`, `core_state_tests.rs`, `layout_mirror_tests.rs` add the Plan-14 `primary_domain_id`, `embedded_domain_ids`, `spatial_placement` fields to every `WindowInfo` initializer. `adapter/layout_tests.rs` resolves the `SplitDirection` ambiguity with an explicit `reovim_subsys_layout::SplitDirection` path on matches. `server_handle_tests.rs` migrates `v2::OptionValue` → `v3::OptionValue`. `clients/tui/tests/sample_module_e2e.rs` (integration test) migrates `reovim_protocol::v2` → `v3` and `grpc.get_mode()` → `grpc.ping()`; the buffer-content assertion is marked `#[ignore]` pending a projection-based buffer query helper (tracked via `tmp/deferral-draft-projection-emitters.md`). `clients/tui/Cargo.toml` adds `reovim-subsys-layout` as a dev-dependency for adapter tests. All 511 `clients/tui` unit tests pass.
  - **V.B.1 bootstrap.rs defect fixes** (Plan 14 Phase I follow-on, surfaced by alias deletion): Plan 14.1's Phase 0 probe missed 11 latent defects in `apps/bin/src/bootstrap.rs` because upstream `clients/tui` compile failures short-circuited `cargo check -p reovim` — with clients/tui now green, they surface. Fixes per #753 E6 intent ("the app layer creates the bridge"): `SyntaxSessionState` relocated to `reovim_driver_text_syntax` import block; `EagerLookupPolicy` imported from `reovim_subsys_input` (domain-neutral `LookupPolicy<CommandId>`) not `reovim_driver_text_input` (old `KeyLookupPolicy`); `ModeEntry::from_info` replaced with explicit `ModeEntry::from_fields(info.id, info.display_name, …)` field unpack; `KeyPolicyAsLookupPolicy` adapter wraps module-registered `KeyLookupPolicy` (e.g. `VimLookupPolicy`) into `LookupPolicy<CommandId>`; `extract_registries` extended to also return `HashMap<CommandId, Arc<dyn CommandHandler>>` (preserves `CommandHandler` that `CommandRegistry::register` upcasts away); `key_sequence_to_input_sequence` bridge (existing `reovim-driver-text-input` helper) converts parsed `KeySequence` to `InputSequence` at the `register_at_layer` call; `HandlerBridge` + `CommandHandlerExecutor` adapter structs bridge `CommandHandler` → `CommandHandle` → `CommandExecutor` for `TextDomainDriver`; `KeymapRegistryAdapter` wraps `KeymapRegistry` and implements driver-tier `KeymapQuery` by routing through `key_sequence_to_input_sequence`; `state.create_buffer(&content)` replaced with the two-step register pattern (`Buffer::from_string` → `TextBufferRegistry::register` → `KernelContext::buffers::register`). `apps/bin/Cargo.toml` adds `reovim-subsys-input` as a direct production dep. `apps/bin/src/bootstrap_tests.rs` destructure updated to the 5-tuple return shape.
  - **V.9 CHANGELOG**: this entry.

- **Plan 15 Phase F + D — workspace final green + regression guards** (#753): two new depgraph guards lock in Plan-15 invariants; bootstrap.rs clippy hygiene; ~220 integration tests marked `#[ignore]` with tracked issue attribution.
  - `lib/depgraph/tests/proto_no_v2_alias.rs` — two-test regression guard. `uapi_protocol_has_no_v2_alias` scans `uapi/protocol/src/lib.rs` for `pub use v3 as v2` (the alias deleted in V.7). `workspace_has_no_live_v2_references` walks the workspace source tree stripping `//` and `/* */` comments and fails on any live `reovim_protocol::v2` or `protocol::v2` reference. Exclusions: `archive/`, `docs/`, `*.md`, `*.proto`, and the test file itself (self-exclusion required because the test body contains the literal pattern strings).
  - `lib/depgraph/tests/net_subsys_has_impl.rs` — single-test regression guard. Enumerates workspace crates via `cargo_metadata`, filters for `reovim-driver-net-*` prefix, asserts at least one declares `reovim-subsys-net` as `DependencyKind::Normal`. Protects the Plan-15 Phase N subsys revival invariant: the subsys is dead code if no in-tree driver implements the contract. Fragile to future net-driver rename — docstring documents the rename protocol.
  - `apps/bin/src/bootstrap.rs` — three clippy fixes: `#[allow(clippy::too_many_lines)]` on three orchestration functions (`bootstrap_runtime`, `extract_registries`, `initialize_modules_with_dependents`) with inline justification comments; `&mut SessionState` → `&SessionState` on `trigger_empty_session_handlers` (mutation not required); `match { Some => …, None => return; }` → `let Some(…) else { return; }` at the `TextBufferRegistry` lookup. No behavior change.
  - Integration test `#[ignore]` attribution: ~160 annotations on `send_keys`-using tests pointing to #759 (raw-bytes codec InvalidArgument, payload-too-short — surfaced by Plan 14 I.5 opaque input codec migration), ~38 on TUI `connect_headless`/`wait_for` tests pointing to #758 (post-Plan-14-E6 TUI render pipeline regression), ~23 pre-existing environment-gated markers carried over. Every `#[ignore]` carries a machine-readable reason string with an issue reference — no silent skips.
  - Workspace clippy/doc cleanup absorbed as part of Phase F: backtick-normalization, `# Panics` doc sections, `const fn` promotions, ~94-file fmt drift sweep.

- **Plan 15 Phase N — net subsys revival** (#753): `reovim-subsys-net` rewritten from line-RPC contracts to tonic-shaped `GrpcServerDriver` async lifecycle; new `ext/server/drivers/net-grpc/` driver; `reovim-server::serve_with_driver` routes through the abstraction.
  - `server/lib/subsys/net/` rewritten: delete `NetDriver` / `TransportListener` / `TransportConnection` / `RpcHandler` / `RpcHandlerContext` / `RpcResult` / `LocalAddr`, `v1::*` JSON-RPC re-exports, and associated `NetError` variants (`ConnectionClosed`, `NotInitialized`, `AlreadyListening`, `ReadFailed`, `WriteFailed`, `JsonError`, `InvalidMessage`). Add `GrpcServerDriver` async trait (`serve(self: Box<Self>, config, router, shutdown) -> Result<(), NetError>` + `bind_handle() -> Arc<ArcSwapOption<SocketAddr>>`), `ShutdownSignal` alias, `TransportKind` tag, `NetError::ServeFailed` + `NetError::UnsupportedTransport`, `From<tonic::transport::Error>`. `PortAllocator` kept. Cargo deps trimmed (`serde`, `serde_json`, `reovim-protocol`, `reovim-arch`, `reovim-kernel` all removed) and `tonic`/`async-trait`/`arc-swap` added. 37 tests preserved + redesigned; `tonic` kept intentionally with justification comment in Cargo.toml.
  - `ext/server/drivers/net-grpc/` (`reovim-driver-net-grpc`, 8 integration tests) — new crate implementing `GrpcServerDriver` over `tokio::net::TcpListener` + `tokio_stream::wrappers::TcpListenerStream` (and `UnixListenerStream` on unix). `TransportConfig::Stdio` returns `NetError::UnsupportedTransport(Stdio)`. Tests cover: TCP bind + bind_handle publication, TCP address-in-use → `BindFailed`, concurrent bind race, Unix socket bind (`cfg(unix)`), stdio rejection, shutdown stops empty router, bind_handle clones share state, invalid host → `InvalidAddress`. Every timing assertion uses a bounded poll, not fixed `sleep`. Optional `grpc-web` feature gates `tonic-web` + `tower-http` deps.
  - `server/lib/server/` — new public `Server::serve_with_driver(driver, shutdown, port_tx) -> Result<(), NetError>` entry point; takes `Box<dyn GrpcServerDriver>` from subsys-net and routes services through it. Spawns a port-reporter task that polls `driver.bind_handle()` and forwards the bound port to the composition root's oneshot. `reovim-subsys-net` added to `[dependencies]` (contract-only). Current `run_grpc`/`run_until`/`run` paths unchanged — they continue to drive tonic directly for gRPC-Web support; migrating them to the driver abstraction is tracked as a follow-on (gRPC-Web layer stacking changes the `Router<L>` generic parameter, which the object-safe trait method cannot accept).
  - `apps/bin/` — composition root adds `reovim-subsys-net` + `reovim-driver-net-grpc` deps and wires integrated mode (`reovim` without subcommand) through `serve_with_driver`. The in-process TUI client uses plain tonic (no gRPC-Web needed), so routing through the abstraction here is lossless. `reovim server` command keeps the existing path pending gRPC-Web migration.

- **Plan 14 Phase D — depgraph enforcement** (#753): four new `cargo_metadata`-based depgraph tests lock in Plan-14 invariants with zero production-code changes.
  - `lib/depgraph/tests/uapi_closed.rs` — `uapi/` membership asserted against an explicit 6-crate allowlist (`reovim-input-codec`, `reovim-content-codec`, `reovim-surface-codec`, `reovim-render-codec`, `reovim-protocol`, `reovim-module-macros`). Adding a new `uapi/` member now requires an explicit edit here, making uapi growth a reviewed checkpoint.
  - `lib/depgraph/tests/codec_module_entry.rs` — every crate under `ext/<concern>-codec/<variant>/` (for concerns input/content/surface/render) must declare `reovim-module-macros` as a production dependency. Guards the codec-lifetime = module-lifetime invariant (missing macros dep = can't `declare_module!` = can't be registered/unregistered symmetrically).
  - `lib/depgraph/tests/codec_tree_isolation.rs` — concern tree membership determined by manifest path prefix `ext/<concern>-codec/`; each codec crate in tree A forbidden from taking production deps on any crate in tree B (B ≠ A). Intra-concern deps remain legitimate. Prevents cross-concern coupling (e.g., a TUI input codec dragging in a PDF content codec).
  - `lib/depgraph/tests/server_no_input_vocab.rs` — `reovim-server` forbidden from production deps on `reovim-input-codec`, `reovim-codec-tui-input`, `reovim-driver-text-input`, and `reovim-subsys-input-contracts`. Regression guard for the input-opacity invariant established by the 2026-04-19 audit and Plan 14 Phase I.
  - `lib/depgraph/tests/core_ext_boundary.rs` — no code change needed; the existing `ext/` catch-all branch already classifies all four new codec trees correctly. Re-verified against the Plan-14 end state.

- **Plan 14 Phase R — render codec reshape + narrow consumer** (#753): three-tier split applied to the render codec concern; pre-existing speculative `uapi/render-codec/` rebuilt as a closed mechanism.
  - `uapi/render-codec/` (`reovim-render-codec`) — rebuilt from scratch. The old speculative shape (`SurfaceKind` enum with CellGrid/Pixel/Vr/Volumetric variants + per-variant `cell_grid.rs` / `pixel.rs` / `vr.rs` / `volumetric.rs` submodules, all with zero consumers) was deleted. The new layout mirrors Phase S: `RenderDescriptor { kind: u16, body: Vec<u8> }`, `render_kind`/`render_body` header accessors, object-safe `Codec` trait (kind/encode/decode), `RenderPayloadError { TooShort | WrongType | InvalidData }` (#[non_exhaustive]), and a single `KIND_CELL_GRID=0x0001` constant for the shipped in-repo codec (0x0001–0x00FF reserved for in-repo, 0x0100–0xFFFF for external).
  - `server/lib/subsys/render-codec/` (`reovim-subsys-render-codec`) — new subsystem crate with `RenderCodecRegistry` trait + `DefaultRenderCodecRegistry` default implementation, keyed by `u16` kind value. Mirrors `SurfaceCodecRegistry` precedent exactly.
  - `ext/render-codec/tui/` (`reovim-render-codec-tui`) — new module crate providing `CellGridRender { width, height, cursor_col, cursor_row, cells: Vec<u8> }` + `CellGridRenderCodec` for `KIND_CELL_GRID`. Body layout: 20-byte big-endian fixed header (width, height, cursor_col, cursor_row, cells_len) followed by `cells_len` opaque cell bytes. Cursor position is validated against dimensions on encode and decode (`InvalidData` on overrun). `TuiRenderCodecModule` holds `Option<Arc<DefaultRenderCodecRegistry>>`; init registers, exit unregisters symmetrically. `declare_module!` gated behind `dynamic` feature.
  - `ext/client/tui/drivers/display/tests/render_codec_consumer.rs` — narrow proof consumer (dev-dependency) implementing `reovim_client_driver::render::RenderTarget`. Wires `CellGridRenderCodec` via `DefaultRenderCodecRegistry` through `submit()` end-to-end. Closes mission-file known-gap #5 ("render-codec has zero consumers") without touching the interactive render hot path; interactive-path migration tracked as follow-on R-b.
  - `clients/lib/driver/src/render.rs` — `RenderTarget::submit` doc comment updated to reference `RenderDescriptor::body` (no API change).

- **Plan 14 Phase S — surface codec reshape** (#753): three-tier split applied to the surface codec concern.
  - `uapi/surface-codec/` (`reovim-surface-codec`) — new closed-mechanism crate holding `SurfaceDescriptor { kind: u16, body: Vec<u8> }`, `surface_kind`/`surface_body` header accessors, well-known kinds for shipped in-repo codecs (`KIND_CELL_GRID=0x0001`, `KIND_PIXEL_BUFFER=0x0002`; 0x0001–0x00FF reserved for in-repo, 0x0100–0xFFFF for external), object-safe `Codec` trait (kind/encode/decode), and `SurfacePayloadError { TooShort | WrongType | InvalidData }`. No geometry or rendering-model vocabulary — concrete surface shapes live in the corresponding `ext/surface-codec/<variant>/` module crates.
  - `server/lib/subsys/surface-codec/` (`reovim-subsys-surface-codec`) — new subsystem crate with `SurfaceCodecRegistry` trait + `DefaultSurfaceCodecRegistry` default implementation, keyed by `u16` kind value, mirroring the input-codec registry precedent.
  - `ext/surface-codec/tui/` (`reovim-surface-codec-tui`) — new module crate providing `CellGridSurface { width, height }` + `CellGridCodec` for `KIND_CELL_GRID`. `TuiSurfaceCodecModule` holds `Option<Arc<DefaultSurfaceCodecRegistry>>`; init registers, exit unregisters symmetrically. `declare_module!` gated behind `dynamic` feature.
  - `ext/surface-codec/pixel/` (`reovim-surface-codec-pixel`) — new module crate providing `PixelSurface { width_px, height_px, dpi }` + `PixelCodec` for `KIND_PIXEL_BUFFER`. Same lifecycle and feature pattern as the TUI variant.
  - `SurfaceDescriptor` migrated out of `reovim-subsys-session` to `uapi/surface-codec/`; consumers import it directly from `reovim_surface_codec`. No re-export remains in `reovim-subsys-session`.
  - `reovim-subsys-layout` stays opaque — no production dep on `reovim-subsys-surface-codec`; the `set_surface(kind, body)` signature keeps primitives, with decode happening at the driver layer via the registry.

- **Plan 14 Phase C — content codec reshape** (#753): three-tier split applied to the content codec concern.
  - `uapi/content-codec/` (`reovim-content-codec`) — new closed-mechanism crate holding domain-neutral `Decode<D>/Encode<D>/Index<D>` traits, `ContentCodec` + `DecodeResult`, type-erased `DecodedEdit` (with `DecodedEdit::Domain(DomainEdit)` replacing the old hardcoded `DecodedEdit::Text` variant), inode/mount primitives, classifier/factory stores, metadata, errors, and annotation data types (moved in from `reovim-driver-annotation::types`).
  - `server/lib/subsys/content-codec/` (`reovim-subsys-content-codec`) — new subsystem crate with `ContentCodecRegistry` trait + `DefaultContentCodecRegistry` default implementation, mirroring the post-#753 input-codec registry pattern.
  - `ext/content-codec/text/` (`reovim-content-codec-text`) — new text-domain carve-out for `TextEdit`, `CodecSessionState`, `MountInfo`, `InodeStaleCheck`, `install_stale_check`, `text_edit_to_decoded_edit`, and domain-text event re-exports. Includes a `MockEdit` integration test that exercises `DomainEdit` downcast roundtrip to prove the codec uapi stays free of text-domain vocabulary.
  - `ext/content-codec/<format>/` — 10 existing codec crates relocated and renamed from `ext/server/drivers|modules/codec-*/` to `ext/content-codec/*/`: `xxd`, `utf8`, `cjk`, `csv`, `hex`, `pdf`, `rlib`, `tar-gz`, `binary-struct`, `legacy`. Package names changed from `reovim-{driver,module}-codec-*` to `reovim-content-codec-*`.
  - `ext/server/drivers/codec/` (`reovim-driver-codec`) — deleted. Consumers now depend directly on `reovim-content-codec` and/or `reovim-content-codec-text`.
  - `reovim-driver-annotation` — `Annotation`, `AnnotationKind`, `AnnotationTarget`, `AnnotationPayload` types moved to `reovim-content-codec`; the driver crate re-exports them to keep its public surface stable.
  - All `use reovim_driver_codec::...` imports must change to `use reovim_content_codec::...` (and `use reovim_content_codec_text::...` for text-specific types). All `use reovim_{driver,module}_codec_*` imports must change to `use reovim_content_codec_*`.

- **depgraph**: Plan 12 Phase 8 adds `layout_no_client` to keep `reovim-subsys-layout` free of direct `reovim-client-model`, `reovim-driver-*`, and `reovim-module-*` manifest dependencies
- **subsys/layout**: Plan 12 Phase 6 removes the `NavigateDirection` alias workspace-wide, switching layout, session, driver, module, and display-facing APIs to the canonical `Direction` type, including `WindowError::NoNeighbor(Direction)` and mirrored compositor errors
- **subsys/layout**: Plan 12 Phase 5 adds raw `set_surface(kind, body)` to `RootCompositor`, keeping surface payloads out of the subsys-layout type boundary while updating only direct compositor implementations and test mocks
- **subsys/layout**: Plan 12 Phase 4 reshapes topology reporting from a single recursive `LayoutTopology` enum into `TiledTree` + `LayerTree` + a struct `LayoutTopology`, adds `RootCompositor::tiled_tree(layer)`, and removes the old sentinel-window topology fallback from the layout module
- **subsys/layout**: Plan 12 Phase 2 removes `focus_at(x, y)` from `RootCompositor`, keeping hit-testing out of the subsys-layout trait surface and limiting fallout to direct compositor implementations, mocks, and tests
- **subsys/layout**: Plan 12 Phase 1 inlines `Rect`, `Size`, `Direction`, and `SplitDirection` into `reovim-subsys-layout`, removing its `reovim-client-model` dependency while preserving the crate-level re-export API for downstream users
- **subsys/session**: Plan 11 Phase 7 extracts `SurfaceDescriptor` and its kind constants from `types.rs` into a dedicated `surface.rs` module, keeping the crate-level re-export stable and leaving the final subsys-session type audit clean
- **server/session**: Plan 11 Phase 6B moves `ChangeSet` ownership out of `reovim-subsys-session` and into `reovim-server`. Server-side gRPC notification/input paths now import `ChangeSet` from `server::session`, while the subsys-session change_set module and tests are removed
- **drivers/text-session**: Plan 11 Phase 6A removes production `ChangeSet` usage from `reovim-driver-text-session`. The transitional bridge now converts `StateChanges` directly into `DispatchResult` / `CommandResult`, eliminating driver dependence on `reovim_subsys_session::change_set::ChangeSet` ahead of the later server ownership move
- **subsys/session + modules**: Plan 11 Phase 5 changes `ExtensionStateBridge::on_cursor_moved` to consume opaque `CursorSnapshot` identity instead of text `(line, col)`. The server now forwards `CursorSnapshot` through bridge hooks, text-session maintains that snapshot alongside its driver-owned text cursor shadow, and `reovim-module-lsp-navigation` hover dismissal now compares opaque cursor bytes instead of text coordinates
- **subsys/session**: Plan 11 Phase 4 removes `Viewport` from the subsys-session `Window` contract. `reovim-subsys-session::Window` now carries only `{ id, buffer_id, domain_id, cursors }`, while text/layout viewport state remains driver-owned in `reovim-driver-text-session`
- **subsys/session**: Plan 11 Phase 3 splits `DomainRouting` out of `DomainDriver`, moving read-only routing queries (`current_mode`, `active_window`, `window_buffer`, `windows`, `window_count`, `active_buffer`) behind the new supertrait while preserving `Arc<dyn DomainDriver>` storage and adding default `cursor_generation()`
- **subsys/session**: Plan 11 Phase 2 removes the remaining text-domain query surface from `reovim-subsys-session`. `DomainDriver` no longer exposes viewport/selection/tab/register/clipboard query methods or their query structs, and `TextDomainDriver` no longer implements that text-specific API
- **drivers/modules**: Phase 10.6 compile fallout now uses a driver-owned `TextCursorShadow` in `reovim-driver-text-session` for text-only bridge consumers. `reovim-module-illuminate` and `reovim-module-lsp-navigation` no longer read text fields from opaque `CursorSnapshot`, and illuminate hold tracking now resets on same-position buffer changes as well as line/column moves
- **subsys/server**: `DomainDriver` now uses `dispatch_input` as its sole dispatch entry. The legacy `dispatch_key`, `dispatch_key_with_extensions`, and `decode_key_fallback` paths were removed, `Session::dispatch_key_for_client` was folded into `dispatch_input_for_client`, and gRPC input now reaches domain drivers only through opaque `InputEvent` dispatch
- **subsys**: `reovim-subsys-input-contracts` crate deleted (Plan 14 I.6). `KeySequence`, `ToKeyToken`, `BindingInfo`, `BindingLayer`, `EagerLookupPolicy`, `KeyLookupPolicy`, `KeyLookupResult`, `KeyLookupState`, `KeymapQuery` moved to `reovim-driver-text-input`. `ModeInfo` moved to `reovim-driver-text-input`. `ModeTransition`, `PopResult`, `TransitionContext` defined in `reovim-driver-text-session::transition` and re-exported from `reovim-driver-text-input`. `InputSequenceBridgeError` and `key_sequence_to_input_sequence` bridge helper live in `reovim-driver-text-input`. All `use reovim_subsys_input_contracts::*` imports must change to `reovim_driver_text_input::*`. `reovim-subsys-session` no longer re-exports `KeySequence`. `reovim-driver-text-session` module `transition` now owns `ModeTransition`/`PopResult`/`TransitionContext` definitions (previously re-exported from input-contracts). Downstream crates `reovim-server`, `reovim-module-vim`, and `reovim-client-tui` (pre-existing breakage) have broken imports that will be fixed in Chunk C
- **server**: Plan 10 Phase 4 import normalization leaves `reovim-server` with only the envelope `InputEvent` sourced from `reovim-subsys-input`; key types stay on `reovim-input-codec` and shared mode/keybinding contracts stay on `reovim-subsys-input-contracts`
- **subsys**: `reovim-subsys-input` is now envelope/header-only. Legacy typed key/mouse defs, clipboard/error compatibility, shared-contract forwards, and their tests were removed; remaining typed consumers now import `KeyEvent`/`KeyCode` from `reovim-input-codec`, while `DomainDriver` keeps its current method shapes for later Plan 11 cleanup
- **drivers**: `reovim-driver-text-input` now owns the driver-private mode/keybinding implementation previously kept in `reovim-subsys-input` (`Keybinding`, lifecycle/provider/store helpers, mode registry/key helpers, and related tests). The driver crate now exposes an explicit façade over driver-private types, `reovim-subsys-input-contracts`, `reovim-input-codec`, and the envelope/header-only `reovim-subsys-input` surface
- **subsys**: New `reovim-subsys-input-contracts` crate now owns the shared driver-free key-sequence, lookup, mode-info, binding-info, and transition contracts used across server/session seams. `reovim-subsys-session` drops its duplicate `KeySequence`, the temporary gRPC notation adapter now lives at a narrow `reovim-input-codec` boundary, and `reovim-depgraph` now enforces zero production `reovim-driver-*` dependencies for `reovim-server`
- **shared/input-codec**: Typed key/mouse vocab ownership now starts in `reovim-input-codec` (`KeyCode`, `KeyEvent`, `KeyEventKind`, `Modifiers`, `KeymapResult`, `Mouse*`, arch conversions). `reovim-subsys-input` retains temporary legacy overlap only for deferred consumers, and `TextDomainDriver::dispatch_input` now decodes codec-local keys before adapting at the legacy boundary. Added exhaustive codec↔legacy parity tests plus `dispatch_input` coverage for valid, malformed, and unsupported payloads
- **server**: Server notification pipeline migrated from text-domain `StateChanges` to domain-neutral `ChangeSet` (sub-plan 05 Phase 2). `build_notifications`, `emit_notifications`, `emit_syntax_updates`, and `notify_codec_indices` all accept `ChangeSet` or domain-neutral parameters. `OptionChange` promoted from `reovim-driver-text-session::api` to `reovim-subsys-session`. Driver-text-session re-exports for backward compatibility
- **subsys**: `ChangeSet` extended with notification-relevant fields: `selection_changed`, `affected_buffers`, `option_changes`, `presence_changed`/`presence_updates`, `extension_changed`/`extensions_updated`, `renamed_buffers`. New `record_*` methods: `record_cursor_move(BufferId)`, `record_selection_change`, `record_option_change`, `record_presence_change`, `record_extension_change`, `record_buffer_renamed`
- **subsys**: Mode transition types promoted from driver crates to `reovim-subsys-input` (sub-plan 05 Phase 3). `TransitionContext`, `PopResult`, `ModeTransition` now live in subsys-input. Both `reovim-driver-text-session` and `reovim-driver-text-input` re-export for backward compatibility. Server imports repointed to subsys-input
- **subsys**: `DomainDriver` trait extended with 7 new methods (sub-plan 05 Phase 4A): `dispatch_key_with_extensions` (primary dispatch with extension pass-through), `current_mode`, `active_window`, `window_buffer`, `windows`, `window_count` (state queries). `DomainStateQuery` extended with `SelectionInfo` struct and `selection_info` method. All new methods have default implementations for backward compatibility
- **drivers**: `TextDomainDriver` implements all 7 new `DomainDriver` methods plus `selection_info`. `dispatch_key_with_extensions` passes server-owned `ExtensionMap` refs through to `TextKeyDispatchProvider`, solving the dual-ownership borrow problem
- **server**: `Session` stores optional `Arc<dyn DomainDriver>` via `set_domain_driver`/`domain_driver()`. `dispatch_key_for_client` delegates to domain driver when wired, falls back to inline `SessionState` path when not wired

- **client**: `CellGridClientModule` extension trait — TUI-specific `ClientModule` extension with `domain_gutter_width()` for domain-aware gutter layout in cell-grid clients
- **client**: `RenderTarget` trait — opaque `submit(bytes)` interface for platform-agnostic render command submission with `RenderError` enum
- **client**: `TextDomainView` domain view module — implements `ClientModule` + `CellGridClientModule`, receives text domain projections, delegates viewport rendering to `DefaultViewportRenderer`
- **client**: `render_unknown_domain_placeholder()` — fallback renderer for unrecognized domains with warning log

### Fixed

- **apps**: `apps/bin/Cargo.toml` declares `reovim-subsys-session` as a direct production dependency. `apps/bin/src/bootstrap.rs` uses `reovim_subsys_session::DomainDriver` but previously resolved the crate only via a transitive build-graph edge; the explicit declaration keeps the build stable against future transitive-dep trimming. New depgraph guard `lib/depgraph/tests/apps_bin_imports_declared.rs` scans `bootstrap.rs` for `reovim_*::` crate-path roots and asserts each is present in `apps/bin`'s `[dependencies]`; prevents regressions of this class (#753)
